import type {Metadata} from "next";import {PageHero} from "@/components/page-hero";import {TestimonialForm} from "@/components/testimonial-form";import {EmptyState} from "@/components/empty-state";import {getApprovedTestimonials} from "@/services";
export const metadata:Metadata={title:"نظرات مشتریان",description:"نظرات و فرم بازخورد مشتریان"};
export default function Testimonials(){const items=getApprovedTestimonials();return <main id="main"><PageHero kicker="بازخورد مشتریان" title="تجربه خود را با ما در میان بگذارید" description="فقط نظرهای تأییدشده نمایش داده می‌شوند. در داده اولیه، نظر تأییدشده‌ای وجود ندارد."/><section className="section-pad"><div className="container demo-split"><div>{items.length?items.map(t=><blockquote key={t.id}>{t.text}</blockquote>):<EmptyState title="هنوز نظری منتشر نشده است" text="برای حفظ صحت محتوا، نمونه‌نظر ساختگی نمایش داده نمی‌شود."/>}</div><TestimonialForm/></div></section></main>}
