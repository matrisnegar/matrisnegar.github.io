import type {Metadata} from "next";import {PageHero} from "@/components/page-hero";import {getServices} from "@/services";
export const metadata:Metadata={title:"خدمات",description:"خدمات استقرار و یکپارچه‌سازی راهکارهای ماتریس"};
export default function Services(){return <main id="main"><PageHero kicker="خدمات" title="از شناخت فرآیند تا بهره‌برداری" description="خدمات پیشنهادی پیرامون استقرار محصولات؛ جزئیات دامنه و قرارداد باید مستقیماً با شرکت تأیید شود."/><section className="section-pad"><div className="container service-grid">{getServices().map(s=><article className="service-card tall" key={s.id}><h2>{s.title}</h2><p>{s.description}</p><h3>فرآیند</h3><ol>{s.process.map(x=><li key={x}>{x}</li>)}</ol></article>)}</div><div className="center"><a className="btn primary" href="/contact">گفتگو درباره نیاز سازمان</a></div></section></main>}
