import type {Metadata} from "next";import {MapPin,Phone,Globe2,AlertTriangle} from "lucide-react";import {PageHero} from "@/components/page-hero";import company from "@/data/company.json";
export const metadata:Metadata={title:"تماس با ما",description:"اطلاعات تماس شرکت داده نگار ماتریس"};
export default function Contact(){return <main id="main"><PageHero kicker="تماس" title="گفتگو را از اینجا آغاز کنید" description="اطلاعات زیر عیناً از صفحات کاتالوگ استخراج شده است."/><section className="section-pad"><div className="container contact-grid"><div className="contact-card"><Phone/><h2>تلفن‌ها</h2>{company.phones.map(x=><a key={x} href={`tel:${x.replace(/[^0-9+]/g,'')}`} dir="ltr">{x}</a>)}</div><div className="contact-card"><Globe2/><h2>وب‌سایت</h2><a href={`https://${company.website}`} target="_blank" rel="noreferrer">{company.website}</a></div>{company.addresses.map(a=><div className="contact-card" key={a}><MapPin/><h2>نشانی درج‌شده در کاتالوگ</h2><p>{a}</p></div>)}</div><div className="container warning"><AlertTriangle/><p>{company.note}</p></div></section></main>}
