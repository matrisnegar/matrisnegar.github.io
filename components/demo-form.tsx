"use client";import {FormEvent,useState} from "react";import {CheckCircle2} from "lucide-react";import {demoRequestService} from "@/services/demoRequestService";import products from "@/data/products.json";
export function DemoForm({compact=false}:{compact?:boolean}){const [sent,setSent]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);demoRequestService.create({fullName:String(f.get("fullName")),company:String(f.get("company")),email:String(f.get("email")),phone:String(f.get("phone")),product:String(f.get("product")),preferredTime:String(f.get("preferredTime")),notes:String(f.get("notes")),contactMethod:String(f.get("contactMethod"))});e.currentTarget.reset();setSent(true)}if(sent)return <div className="success"><CheckCircle2/><h3>درخواست شما ثبت شد.</h3><p>این ثبت در نسخه فعلی فقط روی مرورگر شما نگهداری می‌شود.</p><button className="btn ghost" onClick={()=>setSent(false)}>ثبت درخواست دیگر</button></div>;return <form className={`form-card ${compact?'compact':''}`} onSubmit={submit}><div className="form-grid"><label>نام و نام خانوادگی<input name="fullName" required autoComplete="name"/></label><label>شرکت<input name="company" required autoComplete="organization"/></label><label>ایمیل<input type="email" name="email" required autoComplete="email" dir="ltr"/></label><label>شماره تماس<input type="tel" name="phone" required autoComplete="tel" dir="ltr"/></label><label>محصول موردنظر<select name="product" required defaultValue=""><option value="" disabled>انتخاب کنید</option>{products.map(p=><option key={p.slug} value={p.slug}>{p.name}</option>)}</select></label><label>زمان مناسب تماس<input name="preferredTime" placeholder="مثلاً شنبه، ساعت ۱۰"/></label><label>روش ترجیحی تماس<select name="contactMethod"><option>تماس تلفنی</option><option>ایمیل</option><option>جلسه آنلاین</option></select></label><label className="wide">توضیحات<textarea name="notes" rows={compact?3:5}/></label></div><button className="btn primary" type="submit">ثبت درخواست دمو</button><small>با ثبت فرم، اطلاعات فقط در LocalStorage همین دستگاه ذخیره می‌شود.</small></form>}
